# Project Rules

## Document Conversion & File Processing Rule
- Always use Microsoft `markitdown` (`markitdown <filepath> -o <output.md>`) to convert PDF, Word (.docx), Excel (.xlsx/.xls), PowerPoint (.pptx), HTML, Audio, EPUB, and other structured file formats into Markdown whenever document parsing, text extraction, or conversion to Markdown is requested or required.
